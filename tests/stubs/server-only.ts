/**
 * Test stub for the `server-only` package.
 *
 * In the real app that import makes the build fail if server code is pulled
 * into a client component. Under Vitest there is no such distinction, so we
 * alias it to this empty module (see vitest.config.mts).
 */
export {}
