# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build
/dist

# generated Prisma client (regenerated by `npm run db:generate`)
/src/generated

# testing
/coverage
/test-results
/playwright-report

# ---------------------------------------------------------------------------
# SECRETS — never commit these
# ---------------------------------------------------------------------------
.env
.env.local
.env.*.local
.env.production
*.pem
*-service-account*.json
google-credentials*.json

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# editors / os
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
Thumbs.db

# misc
*.tsbuildinfo
.eslintcache

# stray clones of this repo made into the project folder.
# Committed as gitlinks these produce "unrelated histories" and a broken
# submodule entry on GitHub. Ignored so it cannot happen again.
/Kabirian College PWA
/kabirian-college-pwa

# test harness and browser tests (generated)
/tests/harness/pgdata
/tests/harness/ids*.json
/tests/harness/*.log
/test-results
/playwright-report
/coverage

# local database backups (npm run backup:export) — sensitive, never committed
/backups
